# Create Tasks

Create or update `specs/<number>-<slug>/tasks.md`.

Base it on the matching `plan.md`.

Use checkbox tasks grouped by:

- Implementation
- Tests
- Documentation
- Validation
- Audit

Every task should be concrete enough to verify.
