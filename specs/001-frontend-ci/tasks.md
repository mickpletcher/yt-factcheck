# Tasks

## Implementation

- [x] Rename the Python CI job to `backend`.
- [x] Add a separate `frontend` CI job.
- [x] Install Node.js in the frontend CI job.
- [x] Run `npm ci` from `frontend`.
- [x] Run `npm run build` from `frontend`.

## Documentation

- [x] Update `CHANGELOG.md`.
- [x] Update `assessment.md`.

## Validation

- [x] Run local frontend build.
- [x] Review CI workflow structure.

## Audit

- [x] Confirm the change is scoped to frontend CI and spec workflow setup.
